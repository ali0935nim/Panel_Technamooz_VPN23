# گزارش تغییرات نسخهٔ اصلاح‌شده Panel Technamooz Ultra Release

## اصلاحات انجام‌شده

- اصلاح فراخوانی تمدید کانفیگ و سازگار کردن endpoint با `renew_link_helper(days=...)`.
- پشتیبانی هم‌زمان API عمومی از پارامترهای `p` و `pw` برای جلوگیری از قفل ماندن گروه‌های رمزدار.
- تکمیل schema خروجی صفحهٔ عمومی با `sub_url`، حجم مصرف‌شده، سقف حجم، تعداد اتصال و قالب‌های متنی مصرف.
- اصلاح مسیر XHTTP در خروجی Clash و Sing-box.
- افزودن transport اختصاصی HTTP برای خروجی Sing-box مربوط به XHTTP.
- اصلاح قرارداد بازگشتی `set_link_sub`، `set_link_active` و `remove_sub_group` تا عملیات ربات تلگرام پیام خطای کاذب ندهند.
- هنگام حذف گروه، ارتباط `sub_id` از کانفیگ‌ها نیز پاک می‌شود.
- اعتبارسنجی وجود کانفیگ هنگام افزودن آن به گروه.
- همگام‌سازی روابط `link_ids` و `sub_id` هنگام ویرایش گروه.
- جلوگیری از بازگرداندن hash رمز گروه در پاسخ ویرایش گروه.
- جلوگیری از export شدن token ربات در backup و جلوگیری از import جایگزین‌کنندهٔ آن هنگام restore.
- پذیرش `Content-Type: application/json; charset=utf-8` در endpoint تمدید.
- تغییر پیش‌فرض اعتماد به `X-Forwarded-*` به حالت امن‌تر و غیرفعال؛ فعال‌سازی آن فقط با تنظیم محیطی.
- غیرفعال کردن credentials در CORS wildcard و حفظ credentials فقط برای originهای مشخص.
- ثبت IP در رلهٔ WebSocket فقط پس از عبور از محدودیت IP.
- اضافه شدن `psutil` به وابستگی‌های رسمی.
- escape کردن بخشی از داده‌های کاربر در پیام‌های HTML ربات تلگرام.
- افزودن تست‌های regression برای backend، subscription و callbackهای ربات.

## تست‌های انجام‌شده

- `compileall` برای همهٔ فایل‌های Python: موفق.
- import و route registration: موفق؛ ۴۲ route.
- تمدید کانفیگ: موفق.
- فعال/غیرفعال‌سازی و گروه‌بندی از مسیر helperهای ربات: موفق.
- حذف گروه و پاک شدن ارتباط کانفیگ: موفق.
- unlock صفحهٔ عمومی با `pw`: موفق.
- خروجی Clash و Sing-box برای XHTTP: موفق.
- تست هم‌زمانی تغییر گروه‌ها: موفق.
- تست callbackهای toggle، افزودن گروه و حذف گروه ربات: موفق.
